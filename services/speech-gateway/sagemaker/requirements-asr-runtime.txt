# SageMaker ASR-only 容器的直接依賴。
#
# 這份檔案刻意不包含 TTS 套件，避免第一個 ASR Endpoint 安裝兩套彼此衝突的
# Python 環境。版本固定後，同一個 image tag 才能重建出相同的推論環境。
faster-whisper==1.2.1
Flask==3.1.2
huggingface-hub==0.34.4
numpy==2.1.3
soundfile==0.13.1
